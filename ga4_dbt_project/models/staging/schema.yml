version: 2

models:
  - name: stg_events
    description: "Cleaned staging events"
    columns:
      - name: user_pseudo_id
        tests:
          - not_null
      - name: event_name
        tests:
          - not_null
